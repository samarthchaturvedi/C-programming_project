#include <stdio.h>

int main() {
    char str[100];

    // Input string
    printf("Enter string: ");
    scanf("%s", str);

    // Output string
    printf("You entered: %s\n", str);

    return 0;
}